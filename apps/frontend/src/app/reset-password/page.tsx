"use client";
import { FormEvent,useState } from "react";
import { supabase } from "@/lib/supabase/client";
export default function ResetPasswordPage(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const {error}=await supabase.auth.updateUser({password});setMessage(error?error.message:"Password updated. You can return to Project Atlas.")}return <main className="grid min-h-screen place-items-center bg-[#0a1628] p-6 text-white"><form onSubmit={submit} className="w-full max-w-sm space-y-4 rounded-3xl border border-white/15 bg-[#091018] p-7"><h1 className="text-xl font-bold">Set a new password</h1><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-white/15 bg-black/30 px-3 py-2.5"/><button className="w-full rounded-xl bg-blue-600 py-2.5 font-bold">Update password</button>{message&&<p role="status" className="text-xs text-sky-300">{message}</p>}</form></main>}
