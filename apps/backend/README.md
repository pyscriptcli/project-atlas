# Backend

The standalone authenticated API owns project persistence, storage signing, routing, geocoding, boundaries, and POI provider failover. The frontend communicates only through `/api/v1` and shared contracts.
